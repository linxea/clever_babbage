import React from 'react'

const ShareCalendar = props => (
  <div className="container share-calendar">
    <h1>Send Calendar to Contact:</h1>
    <button>Send</button>
  </div>
)

export default ShareCalendar
